// eslint-disable-next-line import/prefer-default-export
export enum JobStatus {
  STARTED = 'started',
  CONNECTING_WALLET = 'connect wallet',
  WALLET_CONNECTED = 'wallet connected',
  APPROVED = 'approved',
  DECLINED = 'declined',
}
